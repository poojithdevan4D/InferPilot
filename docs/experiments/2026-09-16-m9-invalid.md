# M9 invalid execution record

M9 stopped on its first cell before the server process launched. The runner was invoked through the
development Python 3.14 environment, where `.venv/bin/vllm` does not exist. The preserved result is
`FAILED/FileNotFoundError`; no warm-up, measured window, request, or performance outcome exists.

The failure is non-retryable under the preregistered study rule, so M9 is invalid and will not be
resumed. The manifest and failed run bundle remain under `runs/m9-canary-prefix/` (gitignored).

The driver now performs a side-effect-free preflight for a sibling `vllm` executable before entering
the shared executor. A replacement study must be separately frozen before execution and may not alter
the predictor, SLO, grid, acceptance gates, or evaluation rule based on this infrastructure failure.
